/* 3:lexema nao identificado [ " ] */

string x;
string y = "oi ";
string aux;

main
	write('Digite seu nome: ');
	readln(x);
	aux = y + x;
	writeln ('y ',y);
	writeln (x); /*ver se funciona para imprimir apenas uma variavel */
	writeln (aux);
end